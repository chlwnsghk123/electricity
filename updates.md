# Updates

## v1.20 — 2026-05-24
- **AI 요청에 객관식 셔플 반영**: `currentCard()` 가 사용자에게 보이는 순서대로 `c` 를 재정렬하고 `a` 를 새 위치로 환산해 `/api/ask` 로 전송. 이전엔 원본 인덱스(`q.a`)를 보내 AI 가 "1번/2번..." 이라고 답한 위치가 사용자가 보는 화면 위치와 어긋나는 문제가 있었음. 이제 사용자가 보는 ①②③④ 와 AI 가 언급하는 번호가 일치.
- `fetchQuestionImages(examId, no, choiceCount, order)` 에 선택지 순서 인자 추가 — 셔플된 순서로 선택지 이미지를 첨부해 멀티모달 입력도 일관성 유지.
- 코드: `app.js` `currentCard`·`fetchQuestionImages`·`sendAiMessage`.

## v1.19 — 2026-05-24
- **모의고사 중 메모·AI 질문·태그 칩 숨김**: `state.mode === 'exam'` 일 때 `#notes-section`·`#btn-ai-ask`·`#btn-tag` 를 `.hidden` 처리. 시험 중에는 외부 정보·기존 태그 노출을 차단. 학습·랜덤·복기에서는 모두 그대로 표시.
- **상하 화살표로 선택지 이동**:
  - 모의고사: `↑/↓` 가 즉시 답안을 선택(셔플된 표시 위치 기준, Enter 불필요). 끝에서 반대편으로 wrap.
  - 학습·랜덤·복기: `↑/↓` 는 포커스만 이동. 선택은 Enter/클릭(복기는 읽기 전용).
- 코드 변경: `app.js` `renderDetail` 가시성 토글 + `bindEvents` 키보드 핸들러 확장.

## v1.18 — 2026-05-22
- **객관식 셔플 (정답 위치 암기 방지)**: 문제별로 선택지 순서를 무작위로 섞어 표시. **한 세션 동안 문제별로 고정**(`state.shuffles`), 페이지 재로딩·회차 변경 시 새 순서. 저장은 항상 원본 인덱스(`q.a`와 동일 기준)로 일관성 유지 — `onChooseAnswer`에서 화면 위치 → 원본 인덱스로 변환, 정답 공개/자동 태그/모의고사 채점/복기 모두 원본 기준으로 동작.
- **틀린 문제 일괄 태그 (복기)**: 결과 화면에 «🏷️ 틀린 문제 N개에 태그 일괄 적용» 버튼 추가. 6색 중 선택 → 모든 틀린 문제에 한 번에 적용.
- **필터 결과 일괄 태그 (학습 리스트)**: 태그 필터가 활성화돼 있으면 리스트 상단에 «🏷️ 일괄 태그 적용» 바 표시 → 필터 결과 전체에 일괄 태깅.
- **키보드 화살표 이동**: 상세 화면에서 `←`/`→` 키로 이전/다음 문제 이동. 입력창·시트 열림 시에는 비활성.
- 코드: `app.js`(`renderDetail`·`onChooseAnswer`·`openTagPicker`/`bulkApplyTag`·`bindEvents` 키보드 핸들러), `styles.css`(`.bulk-tag-btn`·`.bulk-tag-bar`).

## v1.17 — 2026-05-15
- **모의고사 복기·기록 기능**: 모의고사를 제출하면 응시 기록(유형·문제셋·내 답안·점수)을 `cbt_exam_history_v1`에 **자동 저장**. 결과 화면에 **O/X 문제 격자**를 추가 — 칸을 누르면 그 문제를 복기.
- **복기 모드(`review`)**: 상세 화면을 재사용해 내 답·정답을 읽기 전용으로 표시하고, 문제별 메모를 작성. `grade()` → 순수 함수 `gradeAttempt(examSet, answers)`로 분리, `finishExam()`으로 제출·시간만료 채점을 공통화.
- **지난 모의고사**: 모드 선택 화면에 4번째 카드 추가 → 저장된 응시 기록 목록(개별 삭제 가능). 새 화면 `#history-view`.
- 코드: `app.js` Part 3~4, `index.html`(모드 카드 + `#history-view`), `styles.css`(`.q-grid`·`.history-item`).

## v1.16 — 2026-05-15
- **신규 학습 페이지 `terms-2022r2.html`**: 전기기사 2022년 2회 핵심 용어 359개를 「견습 전기기사 성장 모험」 스토리로 학습. 5과목 = 5관문, 용어 카드(탭하여 비유 포함 정의 열람), 관문별 10문항 객관식 퀴즈(70%↑ 통과 시 승급), AI 질문(`/api/ask` 재사용)·질문 노트·복기 모드, 진행률 추적·다크 모드 지원.
- 독립 단일 HTML 파일 — 기존 CBT 앱과 분리, localStorage 키 `terms2022r2:*`로 충돌 없음. 용어 데이터는 페이지 내 인라인.
- `index.html` 홈 화면에 진입 배너(`.terms-link`) 추가, `styles.css`에 배너 스타일 추가.

## v1.15 — 2026-05-15
- **AI 입력창 키 동작**: 컴퓨터(비터치 환경)에서 `Enter`로 질문 전송, `Shift+Enter`로 줄바꿈. 터치 기기는 기존대로 `Enter`=줄바꿈 유지(전송 버튼 사용).
- 코드 변경 위치: `app.js` `#sheet-textarea` keydown 핸들러. `window.matchMedia('(pointer: coarse)')`로 터치 여부 판별, IME 조합 중(`isComposing`)에는 전송 안 함.

## v1.14 — 2026-05-02
- **AI 모델 변경**: `MODEL_PRIMARY` `models/gemini-3-flash-preview` → `gemini-2.5-flash` (정식 stable 라인). 폴백도 `gemini-1.5-flash`(폐기 임박) → `gemini-2.0-flash`로 동시 이동해 폴백 자체가 죽을 위험 제거.
- 코드 변경 위치: `api/ask.js` 13~14행. `docs/AI-INTEGRATION.md` §2 동기화.

## v1.13 — 2026-04-22
- **docs 동기화**: 코드 현재 상태와 문서 불일치를 일괄 정리. ARCHITECTURE의 Part 라인 번호·라인 수·history(popstate) 정책, STORAGE의 AI/테마 헬퍼 + state.ai.contextExpanded, UI-MODES의 헤더 다이어그램(태그 칩이 메타줄로 이동) + 랜덤 모드 prev 활성화(이력 기반) + sticky 진행도 바 + 6색 의미·표기색 + 다크 토큰, AI-INTEGRATION의 시스템 프롬프트 갱신본 + 추천 질문 즉시 전송 + 70분 TTL + 컨텍스트 패널 + 답변 시작 스크롤, WORKFLOW의 AI 조정 가이드 보강.
- `index.html` 설정 시트 버전 라벨 v1.10 → v1.12.

## v1.12 — 2026-04-22
- **학습 리스트 카드 태그 위치 이동**: 오른쪽 끝에 있던 색 태그 라벨을 과목 배지(예: `전자기학`) 바로 오른쪽 옆으로 이동. 카드 우측에는 북마크 ★만 유지.
- **팔레트 재배치**:
  - `개념 복습 필요`(내부 코드 `yellow`) 색을 보라(#8b5cf6)로 변경. 다크 모드는 `#b69bff`.
  - 북마크 색을 노랑으로 (`--bookmark: #f5b800`, 다크 `#ffd45e`). 학습 리스트 카드 ★, 상세 헤더 ☆→★, 필터 칩 "★ 북마크" 별에 일괄 적용.
- 해당 색을 사용하던 배경/텍스트 톤을 모두 일치하도록 재조정(라이트/다크).

## v1.11 — 2026-04-22
- **AI 대화 per-문제 TTL 만료**: 각 문제의 AI 대화는 마지막 열람·송수신 시각으로부터 70분이 지나면 해당 문제의 대화만 자동 삭제(다른 문제와 독립). 시트를 다시 열거나 메시지를 주고받을 때마다 TTL이 리셋된다.
- 스키마 변경: `cbt_ai_v1` → `cbt_ai_v2` (`{ [no]: { lastOpenedAt, messages } }`). 상수 `AI_TTL_MS = 70 * 60 * 1000`.
- 만료 판정은 lazy — 시트를 열 때 `getAiMessages(no)` 내부에서 확인 후 즉시 정리.

## v1.10 — 2026-04-22

### 태그·리스트
- **상세 태그 칩을 메타줄로 이동**: 헤더 우상단의 ● 트리거 제거. 대신 과목 배지·No. 오른쪽에 `● 미완료 / 틀림 / 복습 필요 …` 식의 인라인 칩. 태그가 있을 땐 칩 배경·텍스트가 해당 색 톤으로 강조.
- **태그 의미 라벨 정비**: `미완료 / 맞춤 / 틀림 / 복습 필요 / 개념 복습 필요 / 중요`. (기존: 미착수·지우기 / 학습 필요 / 다시 풀기)
- **학습 리스트 카드 우측 라벨 교체**: 📷·∑ 이모지 제거 → 색 태그 의미 텍스트 칩(예: "🔴 틀림" 톤). 카드 한 줄에서 상태가 바로 읽힘.

### AI 시트
- **대화 영속화**: 문제별로 `cbt_ai_v1` 에 저장. 시트를 닫아도 대화 유지, 다음에 같은 문제에서 열면 그대로 복구. 응답 도착 즉시 자동 저장.
- **문제 컨텍스트 확장**: 상단 컨텍스트에 문제 본문 전체 + 본문 이미지(있을 때) + KaTeX 렌더. 길면 2줄로 접고 "펼치기/접기" 토글.
- **추천 질문 즉시 전송**: 클릭하면 텍스트 입력 없이 바로 `sendAiMessage`. 추천 문구도 한 문단·한 줄 중심으로 톤 조정.
- **답변 후 스크롤 보정**: 응답이 끝까지 내려가던 것 → 마지막 AI 메시지의 시작 지점으로 스크롤(`scrollIntoView` 대신 offsetTop 기반 smooth).
- **저장 버튼 가시성 업**: "📝 메모에 저장" 칩을 아웃라인 primary 톤·12px로 확대. 저장 완료 시 체크 + 그린 톤.
- **AI 시스템 프롬프트 간결화**: 총 5~8줄·400자 내외, 결론 한 줄 → 근거 → (필요 시) 핵심 계산 순. "자세히/유도" 명시 요청 시에만 확장.

### 모드·네비게이션
- **랜덤 모드 이전 버튼 작동**: `randomSeen` 이력을 기반으로 prev/next. 이력 끝에서 next는 새 랜덤 픽, 중간이면 이력상 다음으로 이동.
- **Android/브라우저 뒤로가기 라우팅**: 모든 뷰 전환을 `history.pushState` 에 반영. 물리 뒤로가기로 이전 화면 복귀. 모의고사 중 이탈 시 확인 모달, 취소 시 엔트리 복구.
- **시트 뒤로가기**: AI/선택/설정 시트가 열려 있으면 뒤로가기는 시트만 먼저 닫도록 처리(뷰 전환 없음).

### 모의고사
- **진행도 바 sticky**: 상세 상단에 `답한 문제 X / Y · 미답 Z` 실시간 표시. 미답 수는 강조 색.
- **제출 확인 강화**: 미답 수가 있으면 문구에 명시("아직 풀지 않은 문제가 N개 있어요…"). 모든 답 완료 시도 간결히 재확인.

### 테마·환경
- **야간 모드 추가**: 설정 시트에 토글. `data-theme="dark"` 로 Toss 라이트 팔레트의 다크 대응 토큰 적용(배경/서페이스/경계/텍스트/6색 태그/그림자). 선택은 `cbt_settings_v1.theme` 에 저장.
- **Pull-to-refresh 방지**: `html/body { overscroll-behavior-y: contain }` 로 리스트 상단에서 아래로 당겨 새로고침되던 동작 차단.
- **iOS 상단 안전영역**: 앱 헤더 높이에 `env(safe-area-inset-top)` 반영.
- **포커스 링**: `.back-btn/.icon-btn/.tag-inline` 에 `outline: 2px var(--primary)` focus-visible 스타일.

## v1.09 — 2026-04-21
- **문서 구조 리팩토링**: CLAUDE.md를 얇은 진입점으로 재작성. 디테일은 신규 `docs/` 폴더에 분리.
  - `docs/ARCHITECTURE.md` 파일 트리, app.js Part 섹션 맵, 화면 흐름
  - `docs/DATA-SCHEMA.md` manifest · 회차 JSON · LaTeX · 이미지 규칙
  - `docs/STORAGE.md` LS 키 + 메모리 state + 자동 태그 정책 + 진행률 계산
  - `docs/UI-MODES.md` 5화면 · 3모드 · 6색 태그 · 4개 시트 동작
  - `docs/AI-INTEGRATION.md` Gemini 프록시 + 멀티모달 + 환경변수
  - `docs/DEPLOYMENT.md` Vercel + PWA + 체크리스트
  - `docs/WORKFLOW.md` 자주 하는 작업 절차 + 트러블슈팅 + 커밋 규칙
  - `docs/README.md` 인덱스
- **PLAN.md 정리**: 1~5단계 완료 로드맵을 제거하고 "앞으로 할 일"만 남김. 변경 이력은 `updates.md` 한 곳으로 단일화.
- **images/README.md** 갱신: 본문/선택지 이미지 규칙 + 활성 회차 표.
- 코드 변경 없음(문서·구조만). 새 정보가 생기면 CLAUDE.md가 아니라 해당 docs 갱신.

## v1.08 — 2026-04-21
- **AI 버튼 세련화**: 로봇 이모지 제거, `AI에게 질문 →` 아웃라인 프라이머리 버튼으로 교체.
- **추천 질문 톤 조정**: `자세히 알려줘` → `간단히 알려줘`.
- **태그 UX 개편**: 하단 2열 피커를 제거. 상세 뷰 헤더 우측에 색 점 아이콘 트리거 추가 → 클릭 시 6색 팝오버(드롭다운). 선택 즉시 적용 후 자동 닫힘. 외부 클릭/다른 색 선택으로 닫힘.
- **메모 직접 추가**: 메모 섹션 헤더에 `＋` 버튼 → 인라인 텍스트 입력 폼(Ctrl+Enter 저장) → 저장/취소.
- **메모 삭제 제스처화**: 리스트 아이템 내 "삭제" 버튼 제거. 대신 꾹 누르기(터치·마우스 다운 600ms) 또는 우클릭(contextmenu) → 확인 모달 → 삭제. 눌림 상태 시각 피드백.
- **홈·모드 진행률**: 회차 항목의 `날짜 · 100문항` 메타 제거. 대신 `맞춘 수 / 총 문항수` (예: `64 / 100`, green 태그 기준)를 우측에 표시. 모드 선택 화면 메타도 진행률 문구로 대체. 화면 복귀 시 자동 갱신.

## v1.07 — 2026-04-21
- **2025년 복원 5세트 등록**: 2025년 1회(세트1~3), 3회(세트1~2) 각 100문제 활성화. manifest 최상단에 배치.
- **파일명 통일**: `exam_2025_rN.json` → `electricity-2025-rN.json`.
- **문제별 이미지 로직 확장**:
  - 이전: `images/<examId>/<no>.png` (문제 본문 이미지) 하나만 지원.
  - 현재: 본문 `<no>.png` + 각 선택지 `<no>-1.png ~ <no>-N.png` 자동 로드. 파일 존재 기반이라 `has_image` 플래그와 무관하게 작동.
  - 404는 조용히 숨김, 레이아웃 시프트 없음(`onload` 시점에 display 토글).
- **AI 첨부 확장**: `fetchQuestionImages()` 로 본문·선택지 이미지를 모두 수집해 Gemini inlineData 파트에 함께 전송.
- **스타일**: `.choice-content` flex column + `.choice-img`(max-height 180px) 추가.
- 회차별 이미지 목록 README 자동 생성: `images/electricity-2025-rN/README.md`.

## v1.06 — 2026-04-21
- 2022년 2회 데이터 전면 LaTeX 변환: 유니코드 수학 기호(∂, μ, Ω, π, ε, λ, σ, δ, ∇, ⋅, ², ³, ⁻¹, ₀ 등)를 KaTeX용 `$...$` LaTeX 구문으로 치환. 질문 본문 42개 / 선택지 86개 변경.
- 변환 처리: 그리스 문자, 상·하첨자 시퀀스, 루트(`√(...)`→`\sqrt{...}`), 결합 모자(`x̂`→`\hat{x}`), 논리 바(`Ā`→`\bar{A}`), 스크립트 글자(`ℒ`→`\mathcal{L}`), 상첨자 문자(`ᵗ`,`ⁿ` 등). 그리스 문자 + 단일 소문자 경계는 물리 관례상 subscript(`μr`→`\mu_r`)로, 그 외는 공백(`μS`→`\mu S`)으로 분리.
- 한글 사이의 middot(`정·역전`)은 `\cdot`으로 바뀌지 않도록 전처리 유지.
- 한글 혼합 선택지는 토큰별로 수식 부분만 감싸고 본문 텍스트는 그대로 보존.
- 변환 스크립트: `tools/latexify.cjs` (일회성, 재실행 시 동일 결과).

## v1.05 — 2026-04-21
- **홈 구조 개편**: 회차 선택 전용 화면. 2022년 2회만 활성, 나머지는 "준비 중" 라벨로 비활성. 버튼 느낌을 줄여 리스트 아이템 스타일로 전환.
- **모드 선택 화면 분리**: 회차 선택 후 진입. 모드 3개(학습 / 랜덤 학습 / 모의고사). 모든 모드 카드 동일 스타일(primary 강조 제거).
- **모의고사 개편**: 리스트를 건너뛰고 바로 상세 진입. 과목별 20문제(정식) 또는 10문제(하프) 랜덤 추출. 순서도 셔플. 마지막 문제 "시험 제출하기" 버튼. 시간: 정식 150분 / 하프 75분.
- **랜덤 학습 추가**: 과목 선택 → 해당 과목 문제를 무작위 1문제씩 연속 제공. 한 바퀴 돌면 풀 리셋.
- **과목별 모드 제거, 홈의 북마크 단축 진입 메뉴 제거**.
- **설정 시트**: 헤더 우측 ⚙ 버튼 → 설정 바텀시트. "진행도 초기화"는 확인 모달 경유. 기존 하단 초기화 버튼 삭제.
- **6색 태그 + 의미 라벨**: gray(미착수/지우기), green(맞춤), red(틀림), orange(학습 필요), yellow(다시 풀기), blue(중요). 상세 뷰 피커와 리스트 필터 칩에 의미 라벨 노출.
- **자동 태그**: 학습/랜덤 모드에서 정답→green, 오답→red 자동 부여. 모의고사는 제출 시점에 일괄 부여.
- **답 저장 정책 변경**: localStorage에 답을 더 이상 저장하지 않음. 학습/랜덤 모드의 답은 상세 뷰를 벗어나면 소멸. 모의고사 답은 제출까지 메모리에서 유지. (LS 스키마: `cbt_tags_v1` → `cbt_tags_v2`, `cbt_answers_v1` 제거.)
- **메모 기능**: AI 답변 옆 "📝 메모에 저장" 버튼 → 해당 문제의 메모 목록에 저장. 상세 뷰 하단에 메모 목록 + 삭제 버튼. 스키마: `cbt_notes_v1`.
- **KaTeX 수식 렌더**: 문제 본문·선택지·정답 라벨·AI 답변·메모 모두 `$...$`/`$$...$$` 자동 렌더. AI 시스템 프롬프트에도 LaTeX 권장 지시 추가.
- **이미지 폴더 구조**: `images/<examId>/<no>.png`. `has_image: true` 문제는 자동으로 `<img>` 삽입(없으면 숨김). 회차별 필요 이미지 목록을 `images/<examId>/README.md`에 기재.
- **AI 멀티모달**: 문제가 `has_image` 이고 이미지 파일이 존재하면 base64로 읽어 Gemini에 함께 전송.
- **앱 아이콘 + PWA**: `favicon.svg`, `icon.svg`, `manifest.webmanifest` 추가 (블루 배경 + 번개). 간단형.
- **상세 뷰 헤더에 타이머**: 모의고사 모드에서만 ⏱ 표시, 10분 이하 시 경고색.
- **제목 스타일 정리**: 회차 항목을 "버튼"이 아닌 "리스트" 느낌으로 조정.

## v1.04 — 2026-04-21
- Vercel 빌드 실패 수정 시도(vercel.json no-op) → 이후 되돌림.

## v1.03 — 2026-04-21
- `app.js`에 모드 진입(`enterMode`: 학습/과목/모의고사/북마크), 모의고사 150분 타이머(경고색/자동 제출), 이벤트 위임 바인딩 추가
- AI 바텀시트 열기/닫기/드래그 닫기, 추천 질문 3종, 로딩/에러 메시지, 인라인 Markdown 렌더 구현
- `api/ask.js` Vercel Serverless 함수 추가: `VITE_GEMINI_API_KEY`/`GEMINI_API_KEY` 로드, `gemini-3-flash-preview` 우선 + `gemini-1.5-flash` 백업, 문맥 + 히스토리 기반 답변
- `package.json` 추가(`@google/generative-ai` 의존성, `type: module`)
- 레거시 `cbt_full.html` 삭제

## v1.02 — 2026-04-21
- `app.js` 초기 구현(진행 중): 상수/스토리지 키, 전역 state, 유틸(el/debounce/toast/formatTime/renderMarkdownInline), localStorage 헬퍼(per-exam 스코프 북마크/태그/답), 데이터 로드·부트스트랩, 홈/리스트/상세/결과 렌더 함수 추가
- 데이터의 `subject` 번호(1~5) → 이름 매핑을 `state.subjectBy`로 처리

## v1.01 — 2026-04-20
- 데이터 파일을 `data/` 디렉토리로 재배치: `exam_2022_r2.json` → `data/electricity-2022-r2.json`
- `data/index.json` manifest 추가 (회차/시험 확장 대비)
- `updates.md` 초기화

## v1.00 — 2026-04-20
- `CLAUDE.md` 추가 (AI 에이전트 프로토콜, 프로젝트 구조, 핵심 알고리즘, Toss 라이트 테마 원칙)
- 초기 베이스라인: `cbt_full.html` 단일 파일 앱 + 전기기사 2022년 2회 100문제 데이터
